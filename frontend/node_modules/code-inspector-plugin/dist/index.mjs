var m = Object.defineProperty, p = Object.defineProperties;
var d = Object.getOwnPropertyDescriptors;
var l = Object.getOwnPropertySymbols;
var f = Object.prototype.hasOwnProperty, a = Object.prototype.propertyIsEnumerable;
var n = (e, r, o) => r in e ? m(e, r, { enumerable: !0, configurable: !0, writable: !0, value: o }) : e[r] = o, u = (e, r) => {
  for (var o in r || (r = {}))
    f.call(r, o) && n(e, o, r[o]);
  if (l)
    for (var o of l(r))
      a.call(r, o) && n(e, o, r[o]);
  return e;
}, c = (e, r) => p(e, d(r));
import { ViteCodeInspectorPlugin as s } from "@code-inspector/vite";
import b from "@code-inspector/webpack";
import { EsbuildCodeInspectorPlugin as g } from "@code-inspector/esbuild";
import { TurbopackCodeInspectorPlugin as P } from "@code-inspector/turbopack";
import { MakoCodeInspectorPlugin as I } from "@code-inspector/mako";
import { getEnvVariable as k, resetFileRecord as C } from "@code-inspector/core";
import h from "chalk";
import i from "path";
import { fileURLToPath as E } from "url";
function v(e) {
  if (!(e != null && e.bundler)) {
    console.log(
      h.red(
        "Please specify the bundler in the options of code-inspector-plugin."
      )
    );
    return;
  }
  let r = !1;
  e.needEnvInspector && (r = !0, k("CODE_INSPECTOR", process.cwd()) === "true" && (r = !1));
  const o = i.dirname(E(import.meta.url)), t = c(u({}, e), {
    close: r,
    output: i.resolve(o, "./")
  });
  return C(t.output), e.bundler === "webpack" || e.bundler === "rspack" ? new b(t) : e.bundler === "esbuild" ? g(t) : e.bundler === "turbopack" ? P(t) : e.bundler === "mako" ? I(t) : s(t);
}
const M = v;
export {
  v as CodeInspectorPlugin,
  M as codeInspectorPlugin
};
